'use client'

// ModeToggle — artık temayı değil sadece görsel bir element olarak çalışıyor
// Gerçek tema değiştirme Ayarlar sayfasındaki renk seçiciden yapılıyor
export function ModeToggle() {
  return null // ThemeProvider ile tema yönetimi Ayarlar'dan yapılıyor
}